# Add your GitHub personal access token to include private contributions.
# Create one at: https://github.com/settings/tokens (no scopes required)
# Set this in .env.local locally and as GITHUB_TOKEN on Vercel.
GITHUB_TOKEN=
